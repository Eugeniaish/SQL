SELECT 
	r.name,
	SUM(total_amt_usd) AS "Total Amount"
FROM region r
JOIN sales_reps sr ON r.id = sr.region_id
JOIN accounts acct ON sr.id = acct.sales_rep_id
JOIN orders ods ON acct.id = ods.account_id
GROUP BY r.name
ORDER BY "Total Amount";
Description:
Aggregates total sales per region through hierarchical joins across multiple tables.
